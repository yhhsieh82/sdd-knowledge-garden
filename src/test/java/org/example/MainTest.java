package org.example;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class MainTest {

    @Test
    void contextLoads() {
        // This test will fail if the Spring context cannot load
        // No assertions needed - if context loads, test passes
    }
}
