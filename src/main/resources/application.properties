# Query API Runtime Configuration

# Ollama Configuration (runtime settings - vary by environment)
ollama.url=http://localhost:11434
ollama.model=llama3.2:1b
ollama.timeout-seconds=10

# Query Configuration (runtime defaults - not API contract)
query.default-max-sources=10
